<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezen.miracle.mapper.BoardMapper">

<select id="getAll" resultType="com.ezen.miracle.dto.LogoBoardDTO">
select * from logo_board order by board_id desc

</select>

<insert id="write">
insert into logo_board (board_id, board_title,
 board_content, board_category, user_id, view_count, created_at)
 values (board_id_seq.nextval, #{board_title}, #{board_content}, #{board_category}, 1,
 0, systimestamp)
</insert>

<!-- 
	private Integer board_id; 
	private String board_title;
	private String board_content;
	private String board_category;
	private Integer board_good_count;
	private Integer user_id;
	private Integer view_count;
	private Date created_at;
	private Date modified_at;
	private Integer board_show;

 -->

</mapper>